import torch
from torch import nn

class LabelSmoothingLoss(nn.Module):
    def __index__(self):
        pass

    def loss(self, pred, label, smoothed=False):
        if smoothed:

            pass
        else:
            pass

